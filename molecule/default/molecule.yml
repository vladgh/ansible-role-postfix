---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: "${MOLECULE_DISTRO:-ubuntu:20.04}"
    # command: /sbin/init
    # tmpfs:
    #   - /run
    #   - /tmp
    # volumes:
    #   - /sys/fs/cgroup:/sys/fs/cgroup:ro
    # privileged: True
provisioner:
  name: ansible
lint: |
  set -e
  yamllint .
  ansible-lint .
